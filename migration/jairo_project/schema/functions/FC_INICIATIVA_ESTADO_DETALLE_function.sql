-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';

SET search_path = jairo,public;
\set ON_ERROR_STOP ON





CREATE OR REPLACE FUNCTION jairo.fc_iniciativa_estado_detalle (P_ID_DEXEC integer) RETURNS varchar AS $body$
DECLARE
 RET varchar(100);

BEGIN
    SELECT LPEP.DETALLE INTO STRICT RET
        FROM ELISEO.LAMB_PROCESS_EXEC_PASO LPEP
                 INNER JOIN ELISEO.LAMB_PROCESS_PASO LPP ON LPEP.ID_PASO_ACTUAL = LPP.ID_PASO
        WHERE LPEP.ID_DEXEC = P_ID_DEXEC;
return RET;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION jairo.fc_iniciativa_estado_detalle (P_ID_DEXEC integer) FROM PUBLIC;

