-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';

SET search_path = eliseo,public;
\set ON_ERROR_STOP ON

PROCEDURE        SP_ANULAR_SALDO_REG(P_ID_SALDO NUMBER, P_ID_PERSONA NUMBER, P_MOTIVO VARCHAR2, P_ERROR OUT NUMBER, P_MSGERROR OUT VARCHAR2) IS

        L_ACTIVO VARCHAR2(1);
        L_ID_VOUCHER_SALDO NUMBER;

        BEGIN
	        -- VALIDAR SI EL VOUCHER ESTÁ CONTABILIZADO
	        L_ACTIVO := 'S';

            SELECT V.ACTIVO INTO L_ACTIVO FROM VENTA_SALDO D 
            INNER JOIN CONTA_VOUCHER V ON D.ID_VOUCHER = V.ID_VOUCHER 
            WHERE D.ID_SALDO = P_ID_SALDO;

            IF L_ACTIVO = 'S' THEN
            
                SELECT ID_VOUCHER INTO L_ID_VOUCHER_SALDO
                FROM VENTA_SALDO vs
                WHERE ID_SALDO = P_ID_SALDO;
                   
                --UPDATE CONTA_ASIENTO SET IMPORTE = 0, DESCRIPCION = '<< Anulado>>', IMPORTE_ME = 0
                DELETE CONTA_ASIENTO
                WHERE ID_TIPOORIGEN = 2
                AND ID_ORIGEN = P_ID_SALDO
                AND VOUCHER = L_ID_VOUCHER_SALDO;
                  
                --UPDATE VENTA_SALDO SET TOTAL = 0, TOTAL_ME = 0, DETALLE = '<< Anulado >>'
                DELETE VENTA_SALDO
                WHERE ID_SALDO = P_ID_SALDO;
                
                P_ERROR := 0;
                P_MSGERROR := 'OK: Anulado con Exito';
            ELSE
                P_ERROR := 1;
                P_MSGERROR := 'El Voucher ya esta CONTABILIZADO';
            END IF;
    END SP_ANULAR_SALDO_REG;

