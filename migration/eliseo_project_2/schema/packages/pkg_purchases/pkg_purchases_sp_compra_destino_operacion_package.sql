-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,pkg_purchases,public;




CREATE OR REPLACE PROCEDURE eliseo.pkg_purchases_sp_compra_destino_operacion (P_ID_COMPROBANTE text, P_TIPO text, P_BASE bigint, P_IGV bigint, P_OUT_INAFECTA bigint, P_BASE_GRAVADA OUT bigint, P_IGV_GRAVADO OUT bigint, P_BASE_MIXTA OUT bigint, P_IGV_MIXTO OUT bigint, P_BASE_NOGRAVADA OUT bigint, P_IGV_NOGRAVADO OUT bigint, P_BASE_SINCREDITO OUT bigint --, P_BASE_INAFECTA OUT numeric
 , P_OTROS INOUT bigint) AS $body$
BEGIN
            IF P_ID_COMPROBANTE='01' then  -- FACTURA
                IF P_TIPO = 'G' THEN  -- DESTINADAS A OP GRAVADAS
                    P_BASE_GRAVADA :=P_BASE;
                    P_IGV_GRAVADO :=P_IGV;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS     
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'GNG' THEN  -- DESTINADAS A OP GRAVADAS/NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=P_BASE;
                    P_IGV_MIXTO :=P_IGV;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'NG' THEN  -- DESTINADAS A OP NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=P_BASE;
                    P_IGV_NOGRAVADO :=P_IGV;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'ANG' THEN  -- ADQUISICIONES NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_BASE;
                    --P_BASE_INAFECTA := NULL;
                    P_OTROS := NULL;
                END IF;
            ELSIF P_ID_COMPROBANTE='02' then  -- RECIBO POR HONORARIO
                P_BASE_GRAVADA :=NULL;
            ELSIF P_ID_COMPROBANTE='03' then  -- BOLETA DE VENTA
                -- Esta es nuevo rus
                P_BASE_GRAVADA :=NULL;
                P_IGV_GRAVADO :=NULL;
                P_BASE_MIXTA :=NULL;
                P_IGV_MIXTO :=NULL;
                P_BASE_NOGRAVADA :=NULL;
                P_IGV_NOGRAVADO :=NULL;
                P_BASE_SINCREDITO := NULL; -- SOLO PARA NO BOLETAS DE VENTAS
                --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                P_OTROS := P_BASE;
            ELSIF P_ID_COMPROBANTE='04' then  -- LIQUIDACIÓN COMPRA
               IF P_TIPO = 'G' THEN  -- DESTINADAS A OP GRAVADAS
                    P_BASE_GRAVADA :=P_BASE;
                    P_IGV_GRAVADO :=P_IGV;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'GNG' THEN  -- DESTINADAS A OP GRAVADAS/NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=P_BASE;
                    P_IGV_MIXTO :=P_IGV;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'NG' THEN  -- DESTINADAS A OP NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=P_BASE;
                    P_IGV_NOGRAVADO :=P_IGV;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                 ELSIF P_TIPO = 'ANG' THEN  -- ADQUISICIONES NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_BASE;
                    --P_BASE_INAFECTA := NULL;
                    P_OTROS := NULL;
                END IF;

            ELSIF P_ID_COMPROBANTE='05' then  -- BOLETA AEREO
                IF P_TIPO = 'G' THEN  -- DESTINADAS A OP GRAVADAS
                    P_BASE_GRAVADA :=P_BASE;
                    P_IGV_GRAVADO :=P_IGV;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'GNG' THEN  -- DESTINADAS A OP GRAVADAS/NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=P_BASE;
                    P_IGV_MIXTO :=P_IGV;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'NG' THEN  -- DESTINADAS A OP NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=P_BASE;
                    P_IGV_NOGRAVADO :=P_IGV;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'ANG' THEN  -- ADQUISICIONES NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_BASE;
                    --P_BASE_INAFECTA := NULL;
                    P_OTROS := NULL;
                END IF;
            ELSIF P_ID_COMPROBANTE='07' OR P_ID_COMPROBANTE='08' OR P_ID_COMPROBANTE='87' OR P_ID_COMPROBANTE='88' then
                -- NOTA DE CREDITO, NOTA DE DEBITO, NOTA DE CREDITO ESPECIAL, NOTA DE DEBITO ESPECIAL
                IF P_TIPO = 'G' THEN  -- DESTINADAS A OP GRAVADAS
                    P_BASE_GRAVADA :=P_BASE;
                    P_IGV_GRAVADO :=P_IGV;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'GNG' THEN  -- DESTINADAS A OP GRAVADAS/NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=P_BASE;
                    P_IGV_MIXTO :=P_IGV;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'NG' THEN  -- DESTINADAS A OP NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=P_BASE;
                    P_IGV_NOGRAVADO :=P_IGV;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'ANG' THEN  -- ADQUISICIONES NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_BASE;
                    --P_BASE_INAFECTA := NULL;
                    P_OTROS := NULL;
                END IF;
            -- ELSIF P_ID_COMPROBANTE='08' then -- NOTA DE DEBITO
            -- ELSIF P_ID_COMPROBANTE='87' then -- NOTA DE CREDITO ESPECIAL
            -- ELSIF P_ID_COMPROBANTE='88' then -- NOTA DE DEBITO ESPECIAL
            ELSIF P_ID_COMPROBANTE='14' then  -- SERVICIOS PUBLICO
                IF P_TIPO = 'G' THEN  -- DESTINADAS A OP GRAVADAS
                    P_BASE_GRAVADA :=P_BASE;
                    P_IGV_GRAVADO :=P_IGV;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS  
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'GNG' THEN  -- DESTINADAS A OP GRAVADAS/NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=P_BASE;
                    P_IGV_MIXTO :=P_IGV;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS 
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'NG' THEN  -- DESTINADAS A OP NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=P_BASE;
                    P_IGV_NOGRAVADO :=P_IGV;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS  
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'ANG' THEN  -- ADQUISICIONES NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_BASE;
                    --P_BASE_INAFECTA := NULL;
                    P_OTROS := NULL;
                END IF;
            ELSIF P_ID_COMPROBANTE='12' then  -- TICKET O CINTA EMITIDA
               IF P_TIPO = 'G' THEN  -- DESTINADAS A OP GRAVADAS
                    P_BASE_GRAVADA :=P_BASE;
                    P_IGV_GRAVADO :=P_IGV;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'GNG' THEN  -- DESTINADAS A OP GRAVADAS/NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=P_BASE;
                    P_IGV_MIXTO :=P_IGV;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'NG' THEN  -- DESTINADAS A OP NO GRAVADAS
                    P_BASE_GRAVADA := NULL;
                    P_IGV_GRAVADO := NULL;
                    P_BASE_MIXTA := NULL;
                    P_IGV_MIXTO := NULL;
                    P_BASE_NOGRAVADA := P_BASE;
                    P_IGV_NOGRAVADO := P_IGV;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'ANG' THEN  -- ADQUISICIONES NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_BASE;
                    --P_BASE_INAFECTA := NULL;
                    P_OTROS := NULL;
                END IF;
            ELSIF P_ID_COMPROBANTE='15' then  -- BOLETA SERVICIO TRANSPORTE TERRESTRE
              IF P_TIPO = 'G' THEN  -- DESTINADAS A OP GRAVADAS
                    P_BASE_GRAVADA := P_BASE;
                    P_IGV_GRAVADO := P_IGV;
                    P_BASE_MIXTA := NULL;
                    P_IGV_MIXTO := NULL;
                    P_BASE_NOGRAVADA := NULL;
                    P_IGV_NOGRAVADO := NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'GNG' THEN  -- DESTINADAS A OP GRAVADAS/NO GRAVADAS
                    P_BASE_GRAVADA := NULL;
                    P_IGV_GRAVADO := NULL;
                    P_BASE_MIXTA := P_BASE;
                    P_IGV_MIXTO := P_IGV;
                    P_BASE_NOGRAVADA := NULL;
                    P_IGV_NOGRAVADO := NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'NG' THEN  -- DESTINADAS A OP NO GRAVADAS
                    P_BASE_GRAVADA := NULL;
                    P_IGV_GRAVADO := NULL;
                    P_BASE_MIXTA := NULL;
                    P_IGV_MIXTO := NULL;
                    P_BASE_NOGRAVADA := P_BASE;
                    P_IGV_NOGRAVADO := P_IGV;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'ANG' THEN  -- ADQUISICIONES NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_BASE;
                    --P_BASE_INAFECTA := NULL;
                    P_OTROS := NULL;
                END IF;
            ELSIF P_ID_COMPROBANTE='13' then  -- DOCUMENTO SISTEMA FINANCIERO SEGUROS
               IF P_TIPO = 'G' THEN  -- DESTINADAS A OP GRAVADAS
                    P_BASE_GRAVADA := P_BASE;
                    P_IGV_GRAVADO := P_IGV;
                    P_BASE_MIXTA := NULL;
                    P_IGV_MIXTO := NULL;
                    P_BASE_NOGRAVADA := NULL;
                    P_IGV_NOGRAVADO := NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'GNG' THEN  -- DESTINADAS A OP GRAVADAS/NO GRAVADAS
                    P_BASE_GRAVADA := NULL;
                    P_IGV_GRAVADO := NULL;
                    P_BASE_MIXTA := P_BASE;
                    P_IGV_MIXTO := P_IGV;
                    P_BASE_NOGRAVADA := NULL;
                    P_IGV_NOGRAVADO := NULL;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'NG' THEN  -- DESTINADAS A OP NO GRAVADAS
                    P_BASE_GRAVADA := NULL;
                    P_IGV_GRAVADO := NULL;
                    P_BASE_MIXTA := NULL;
                    P_IGV_MIXTO := NULL;
                    P_BASE_NOGRAVADA := P_BASE;
                    P_IGV_NOGRAVADO := P_IGV;
                    P_BASE_SINCREDITO := P_OUT_INAFECTA; -- SOLO PARA NO BOLETAS DE VENTAS
                    --P_BASE_INAFECTA := NULL; -- SOLO PARA BOLETAS DE VENTAS
                    P_OTROS := P_OTROS;
                ELSIF P_TIPO = 'ANG' THEN  -- ADQUISICIONES NO GRAVADAS
                    P_BASE_GRAVADA :=NULL;
                    P_IGV_GRAVADO :=NULL;
                    P_BASE_MIXTA :=NULL;
                    P_IGV_MIXTO :=NULL;
                    P_BASE_NOGRAVADA :=NULL;
                    P_IGV_NOGRAVADO :=NULL;
                    P_BASE_SINCREDITO := P_BASE;
                    --P_BASE_INAFECTA := NULL;
                    P_OTROS := NULL;
                END IF;
            ELSIF P_ID_COMPROBANTE='16' then  -- 16-Boletos de viaje emitidos por las empresas de transporte nacional de pasajeros
               -- ESTA ES ADQUISICIONES NO GRAVADAS
                P_BASE_GRAVADA :=NULL;
                P_IGV_GRAVADO :=NULL;
                P_BASE_MIXTA :=NULL;
                P_IGV_MIXTO :=NULL;
                P_BASE_NOGRAVADA :=NULL;
                P_IGV_NOGRAVADO :=NULL;
                P_BASE_SINCREDITO := P_BASE;
                --P_BASE_INAFECTA := NULL;
                P_OTROS := NULL;
            END IF;

    END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON PROCEDURE eliseo.pkg_purchases_sp_compra_destino_operacion (P_ID_COMPROBANTE text, P_TIPO text, P_BASE bigint, P_IGV bigint, P_OUT_INAFECTA bigint, P_BASE_GRAVADA OUT bigint, P_IGV_GRAVADO OUT bigint, P_BASE_MIXTA OUT bigint, P_IGV_MIXTO OUT bigint, P_BASE_NOGRAVADA OUT bigint, P_IGV_NOGRAVADO OUT bigint, P_BASE_SINCREDITO OUT bigint  , P_OTROS INOUT bigint) FROM PUBLIC;
