-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,utils,public;




CREATE OR REPLACE FUNCTION eliseo.utils_substr_ (P_EXPR text,P_PRECISION bigint DEFAULT NULL) RETURNS char AS $body$
BEGIN
  IF coalesce(P_PRECISION::text, '') = '' THEN
    RETURN SUBSTR(P_EXPR,1,current_setting('utils.default_char_size')::bigint);
  ELSE
    RETURN SUBSTR(P_EXPR,1,P_PRECISION);
  END IF;
END;
--- MASTER CONVERT_TO_CHAR for DATETIME values
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION eliseo.utils_substr_ (P_EXPR text,P_PRECISION bigint DEFAULT NULL) FROM PUBLIC;
