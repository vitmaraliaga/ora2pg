-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,utils,public;




CREATE OR REPLACE FUNCTION eliseo.utils_isdate (p_expr text) RETURNS bigint AS $body$
DECLARE

     v_is_valid_date integer := 0;

BEGIN
    IF (utils_convert_string_to_timestamp(p_expr) IS NOT NULL AND (utils_convert_string_to_timestamp(p_expr))::text <> '') THEN
       RETURN 1;
    ELSE
       SELECT (CASE WHEN (TO_DATE(p_expr) IS NOT NULL AND (TO_DATE(p_expr))::text <> '') THEN 1 ELSE 0 END) INTO STRICT v_is_valid_date;
       RETURN v_is_valid_date;
    END IF;
EXCEPTION 
    WHEN OTHERS THEN
       RETURN 0;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION eliseo.utils_isdate (p_expr text) FROM PUBLIC;
