-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,utils,public;




CREATE OR REPLACE FUNCTION eliseo.utils_datediff_ (p_datepart text, p_start_date_expr TS, p_end_date_expr TS) RETURNS bigint AS $body$
BEGIN
 IF current_setting('utils.database_type')::varchar(10) = current_setting('utils.sybase')::varchar(10) THEN
    RETURN utils_datediff_sybase(UPPER(p_datepart),p_start_date_expr,p_end_date_expr);
 ELSE
    RETURN utils_datediff_sqlserver(p_datepart,p_start_date_expr,p_end_date_expr);
 END IF;
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION eliseo.utils_datediff_ (p_datepart text, p_start_date_expr TS, p_end_date_expr TS) FROM PUBLIC;
