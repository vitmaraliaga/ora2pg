-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,utils,public;




CREATE OR REPLACE FUNCTION eliseo.utils_round_ (p_expr bigint, p_len bigint, p_function bigint DEFAULT 0) RETURNS bigint AS $body$
DECLARE

    v_ret_value bigint;

BEGIN
      IF p_function = 0 THEN
         v_ret_value := ROUND(p_expr, p_len);
      ELSE
         v_ret_value := date_trunc(p_len, p_expr);
      END IF;

      RETURN v_ret_value;
EXCEPTION
     WHEN OTHERS THEN
       RAISE EXCEPTION '%', DBMS_UTILITY.FORMAT_ERROR_STACK USING ERRCODE = '45000';
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION eliseo.utils_round_ (p_expr bigint, p_len bigint, p_function bigint DEFAULT 0) FROM PUBLIC;
