-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,utils,public;




CREATE OR REPLACE FUNCTION eliseo.utils_degrees (p_angle_radians bigint) RETURNS bigint AS $body$
BEGIN
    IF coalesce(p_angle_radians::text, '') = '' THEN
      RETURN NULL;
    END IF;

    RETURN ROUND(p_angle_radians / utils_pi() * 180);
EXCEPTION
    WHEN OTHERS THEN
      RAISE EXCEPTION '%', DBMS_UTILITY.FORMAT_ERROR_STACK USING ERRCODE = '45000';
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION eliseo.utils_degrees (p_angle_radians bigint) FROM PUBLIC;
