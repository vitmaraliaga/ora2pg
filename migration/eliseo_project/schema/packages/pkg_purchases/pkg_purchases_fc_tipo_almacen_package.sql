-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,pkg_purchases,public;




CREATE OR REPLACE FUNCTION eliseo.pkg_purchases_fc_tipo_almacen (P_ID_ALMACEN bigint) RETURNS bigint AS $body$
DECLARE

        L_ID_EXISTENCIA bigint;

BEGIN
            SELECT coalesce(ID_EXISTENCIA,1) INTO STRICT L_ID_EXISTENCIA
            FROM INVENTARIO_ALMACEN
            WHERE ID_ALMACEN = P_ID_ALMACEN;

            RETURN(L_ID_EXISTENCIA);
    END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION eliseo.pkg_purchases_fc_tipo_almacen (P_ID_ALMACEN bigint) FROM PUBLIC;
