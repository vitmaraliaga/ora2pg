-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,utils,public;


-- PUBLIC NOT REFACTORED YET
CREATE OR REPLACE FUNCTION eliseo.utils_rand (p_seed bigint DEFAULT NULL) RETURNS bigint AS $body$
DECLARE

    v_rand_num bigint;

BEGIN
      IF (p_seed IS NOT NULL AND p_seed::text <> '') THEN
--          DBMS_RANDOM.SEED(p_seed);
      END IF;

--       v_rand_num := DBMS_RANDOM.VALUE();

      RETURN v_rand_num;
EXCEPTION
     WHEN OTHERS THEN
       RAISE EXCEPTION '%', DBMS_UTILITY.FORMAT_ERROR_STACK USING ERRCODE = '45000';
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
;
-- REVOKE ALL ON FUNCTION eliseo.utils_rand (p_seed bigint DEFAULT NULL) FROM PUBLIC;
