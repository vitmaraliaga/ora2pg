-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,utils,public;




CREATE OR REPLACE FUNCTION eliseo.utils_convert_string_to_timestamp (ARG text) RETURNS TS AS $body$
BEGIN
   FOR i in current_setting('utils.dt_formats')::VARCHAR2_ARRAY.FIRST .. current_setting('utils.dt_formats')::VARCHAR2_ARRAY.LAST
   LOOP
    BEGIN
      RETURN to_timestamp(ARG,current_setting('utils.dt_formats')::VARCHAR2_ARRAY(i));
   EXCEPTION
   WHEN OTHERS THEN
      NULL; -- Keep Trying
    END;
   END LOOP;
   --Attempt to cast one last time, but its really to designed to throw the error to the application when a string is not recognized
   RETURN  to_timestamp(ARG,current_setting('utils.dt_formats')::VARCHAR2_ARRAY(1));
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION eliseo.utils_convert_string_to_timestamp (ARG text) FROM PUBLIC;
