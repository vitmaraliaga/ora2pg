-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';
SET search_path = eliseo,pkg_caja,public;




CREATE OR REPLACE FUNCTION eliseo.pkg_caja_fc_vale_comprobante (P_ID_VALE bigint) RETURNS varchar AS $body$
DECLARE

        L_IMPORTE decimal(10,2);

BEGIN
            SELECT SUM(IMPORTE) AS IMPORTE INTO STRICT L_IMPORTE
            FROM PEDIDO_COMPRA WHERE ID_VALE = P_ID_VALE;

            RETURN(L_IMPORTE);
    END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION eliseo.pkg_caja_fc_vale_comprobante (P_ID_VALE bigint) FROM PUBLIC;
