-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';

SET search_path = eliseo,public;
\set ON_ERROR_STOP ON





CREATE OR REPLACE FUNCTION eliseo.fc_conta_voucher (P_ID_VOUCHER integer) RETURNS varchar AS $body$
DECLARE

L_NOMBRE varchar(100);

BEGIN
        SELECT
                NUMERO into STRICT L_NOMBRE
        FROM CONTA_VOUCHER
        WHERE ID_VOUCHER = P_ID_VOUCHER;

        RETURN(L_NOMBRE);

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION eliseo.fc_conta_voucher (P_ID_VOUCHER integer) FROM PUBLIC;

