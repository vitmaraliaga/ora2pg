-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';

SET search_path = eliseo,public;
\set ON_ERROR_STOP ON





CREATE OR REPLACE FUNCTION eliseo.fc_comprobante_venta (P_ID_VENTA bigint) RETURNS varchar AS $body$
DECLARE

    L_ID_COMPROBANTE varchar(2);

BEGIN 
       
    SELECT
            coalesce(ID_COMPROBANTE,ID_COMPROBANTE_REF) INTO STRICT L_ID_COMPROBANTE
    FROM VENTA
    WHERE ID_VENTA = P_ID_VENTA;
    RETURN(L_ID_COMPROBANTE);
END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION eliseo.fc_comprobante_venta (P_ID_VENTA bigint) FROM PUBLIC;

