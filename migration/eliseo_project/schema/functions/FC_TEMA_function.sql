-- Generated by Ora2Pg, the Oracle database Schema converter, version 25.0
-- Copyright 2000-2025 Gilles DAROLD. All rights reserved.
-- DATASOURCE: dbi:Oracle:host=10.174.11.5;sid=upeu;port=1521

SET client_encoding TO 'UTF8';

SET search_path = eliseo,public;
\set ON_ERROR_STOP ON





CREATE OR REPLACE FUNCTION eliseo.fc_tema (P_ID_TEMA integer) RETURNS varchar AS $body$
DECLARE

L_NOMBRE varchar(100);

BEGIN
        SELECT NOMBRE INTO STRICT L_NOMBRE
        FROM LAMB_TEMA
        WHERE ID_TEMA = P_ID_TEMA;

        RETURN(L_NOMBRE);

END;
$body$
LANGUAGE PLPGSQL
SECURITY DEFINER
 STABLE;
-- REVOKE ALL ON FUNCTION eliseo.fc_tema (P_ID_TEMA integer) FROM PUBLIC;

